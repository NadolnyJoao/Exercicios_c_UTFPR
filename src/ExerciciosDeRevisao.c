#include <stdio.h>

void main(){

//Exercicio 1;
/*
int quantidade;
float preco, preco_final, preco_parcelado, capital;

    printf("informe a quantidade e o preco do produto\n");
    scanf("%d %f", &quantidade, &preco);
    preco *= quantidade;
    preco_final = preco;
    if (quantidade >= 5 && quantidade < 10){
        preco_final -= preco * 0.05;


    }
    else if(quantidade >= 10){
        preco_final -= preco * 0.1;

    }
    preco_parcelado = preco_final;
    preco_final -= preco_final * 0.03;
    printf("\nPreco a vista: %.2f, Parcelado em 3x: %.2f, valor de cada parcela: %.2f",preco_final, preco_parcelado, preco_parcelado/3.0);
    printf("\nInforme qual a quantia disponivel para a compra, por favor\n");
    scanf("%f", &capital);
    if (capital >= preco_final){
        printf("Voce pode pagar a vista ou parcelar!");
    }else if (capital < preco_final && capital >= preco_parcelado/3){
        printf("\nVoce pode parcelar!");
    }else {
        printf("Voce nao pode pagar nem a vista, nem nenhuma parcela");
    }
    */

//Exercicio 2; 

    int nUsr, qnt20, qnt15, qnt10, qnt5; 
    scanf("%d %d %d %d %d ", &nUsr, qnt20, qnt15, qnt10, qnt5);
    






}
